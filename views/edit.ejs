<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="bg-zinc-900">
    <div class="w-full h-full p-10 text-white">
        <div class="flex justify-between mb-3">
            <a class="px-5 text-sm py-2 text-black bg-sky-500 rounded-lg" href="/profile">Go Back</a>
        </div>
        <h1 class="text-3xl mb-8"><span class="font-light">Hello,</span> <%= user.name %> 👋🏻</h1>
        <h5>Update Your Post.</h5>
        <form method="post" action="/update/<%= post._id %>">
            <textarea class="resize-none block w-1/3 mt-5 p-3 bg-transparant border-2 border-zinc-100 rounded-lg" name="content"><%= post.content %></textarea>
            <button class="mt-3 bg-yellow-500 text-black px-5 py-2 rounded-lg text-sm" type="submit">Update Post</button>
        </form>
    </div>
</body>
</html>